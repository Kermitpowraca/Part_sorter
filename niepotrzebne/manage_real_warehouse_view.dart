import 'package:flutter/material.dart';
import 'package:flutter_translate/flutter_translate.dart';

class ManageRealWarehouseView extends StatelessWidget {
  const ManageRealWarehouseView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(translate('manageRealWarehouseContent')), // Placeholder
    );
  }
}
